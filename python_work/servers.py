#Loop the list with some values
servers = ['server_a', 'server_b', 'server_c', 'server_d']
for server in servers:
	print(server)


 
#Loop range of numbers
for x in range(1,10):
	print(x)

#Create numeric list
numbers = list(range(1,11))
print(numbers)
